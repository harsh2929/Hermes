class ChatUser {
  constructor(id, name, desc, images, userId) {
    this.id = id;
    this.name = name;
    this.desc = desc;
    this.images = images;
    this.userId = userId;
  }
}

export default ChatUser;
